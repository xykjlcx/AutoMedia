# AutoMedia v2 完成报告

**日期：** 2026-04-05  
**范围：** Phase 1-4 全部完成  
**构建状态：** ✅ `pnpm build` 通过 | ✅ `pnpm test` 12/12 通过  
**总变更：** 93 个文件，+12,093 行，21 个 commit

---

## Phase 1：内容创作 ✅

### 交付功能
- **内容工作台 `/studio`** — 全屏 Markdown 编辑器 + 可收起素材/预览侧栏
- **AI 内容生成** — 小红书帖 / Twitter Thread / 公众号文章三平台模板
- **分享卡片** — satori 渲染 PNG（AI 生成文案 + 卡片渲染），预留 AI 生图扩展口
- **HTML/Markdown 导出** — 带样式的 HTML 文件下载
- **日报页多选** — 勾选文章 → 发送到 Studio
- **用户行为追踪** — `user_events` 通用事件表 + `/api/events` API
- **DDD 目录重组** — `digest/` + `studio/` + `ai/` 按域组织
- **批处理工具** — `ai/batch.ts` 通用批处理函数，scoring/summarize 已用其重构

### 新增文件
```
src/lib/studio/          — 6 个文件（generator, platforms/*, card-renderer, exporter, queries）
src/app/api/studio/      — 5 个路由（drafts CRUD, generate, cards, export）
src/components/studio/   — 6 个组件（studio-page, draft-editor, source-picker, platform-selector, card-preview, export-dialog）
src/lib/analytics/       — track.ts
src/lib/ai/batch.ts      — 通用批处理
```

### 新增数据表
- `drafts` — 内容草稿
- `draft_sources` — 草稿素材关联
- `share_cards` — 分享卡片记录
- `user_events` — 用户行为事件

---

## Phase 2：智能洞察 ✅

### 交付功能
- **知识图谱 `/insights`** — 从文章中提取实体（人物/公司/产品/技术），卡片网格可视化，按类型过滤
- **破圈预警** — 检测话题跨源扩散（如 HN → 36氪 → 知乎），展示传播路径
- **对比日报** — 同一事件不同源的观点对比，AI 生成多角度分析
- **Pipeline 集成** — 实体提取作为 Stage 3.6 嵌入采集管线

### 新增文件
```
src/lib/digest/          — entity-extract.ts, cross-source-alert.ts, compare-views.ts
src/app/api/insights/    — 4 个路由（graph, alerts, extract, compare/[clusterId]）
src/components/insights/ — 3 个组件（entity-graph, cross-source-alerts, compare-views）
src/app/insights/        — page.tsx
```

### 新增数据表
- `topic_entities` — 实体（人物/公司/产品/技术）
- `article_relations` — 文章-实体关联

---

## Phase 3：数据体验 ✅

### 交付功能
- **阅读统计仪表盘 `/dashboard`** — 4 个摘要卡片 + 每日阅读柱状图 + 来源分布 + 24h 热力图 + 热门文章
- **阅读位置记忆** — localStorage 缓存 + DB 持久化，debounce 500ms，日报页自动恢复位置
- **零新依赖** — 图表全部用 CSS div + Tailwind 实现，未引入 recharts

### 新增文件
```
src/lib/analytics/stats.ts           — 统计聚合（6 个 SQL 查询）
src/components/dashboard/            — dashboard-page.tsx（460 行，纯 CSS 图表）
src/components/hooks/                — use-reading-position.ts
src/app/api/analytics/stats/         — route.ts
src/app/api/reading-position/        — route.ts
src/app/dashboard/                   — page.tsx
```

### 新增数据表
- `reading_position` — 阅读位置记忆

---

## Phase 4：发现 ✅

### 交付功能
- **RSS 源推荐** — AI 根据用户偏好推荐新源，42 个内置高质量源目录
- **设置页集成** — 源管理新增「发现」Tab，推荐卡片 + 一键添加 + 忽略
- **分类覆盖** — AI/ML、跨境电商、技术开发、创业/产品、综合资讯

### 新增文件
```
src/lib/discovery/          — catalog.ts（42 个源）, recommend.ts（AI 推荐引擎）
src/app/api/discovery/      — 4 个路由（suggestions, refresh, add, dismiss）
```

### 新增数据表
- `source_suggestions` — 推荐源记录

---

## 架构变更总结

### 目录结构（DDD 风格）
```
src/lib/
├── ai/           ← 通用 AI 能力（client, batch, utils）
├── digest/       ← Digest 域（pipeline, scoring, clustering, summarize, trends, preference, entity-extract, cross-source-alert, compare-views）
├── studio/       ← Studio 域（generator, platforms/*, card-renderer, exporter, queries）
├── analytics/    ← Analytics 域（track, stats）
├── discovery/    ← Discovery 域（catalog, recommend）
└── db/           ← 数据库层
```

### 新增页面
| 路径 | 功能 | Phase |
|------|------|-------|
| `/studio` | 内容工作台 | 1 |
| `/insights` | 智能洞察（知识图谱+预警+对比） | 2 |
| `/dashboard` | 阅读统计仪表盘 | 3 |

### 新增 API 路由（17 个）
- `/api/studio/*` — 草稿 CRUD + AI 生成 + 卡片 + 导出（5 个）
- `/api/events` — 用户事件上报（1 个）
- `/api/insights/*` — 知识图谱 + 预警 + 对比（4 个）
- `/api/analytics/stats` — 阅读统计（1 个）
- `/api/reading-position` — 阅读位置（1 个）
- `/api/discovery/*` — 源推荐（4 个）

### 新增数据表（8 张）
drafts, draft_sources, share_cards, user_events, topic_entities, article_relations, reading_position, source_suggestions

### 新增依赖
- `satori` — JSX → SVG 渲染
- `@resvg/resvg-js` — SVG → PNG 转换

---

## 导航栏最终状态

```
今日日报 | 历史 | 收藏 | 汇总 | 洞察 | 搜索 | Studio | 数据 | 设置
```

---

## 已知限制 & 后续改进方向

1. **分享卡片字体** — 需要手动下载 NotoSansSC-Regular.ttf 到 `public/fonts/`，否则 fallback 到系统字体
2. **私域源采集** — 架构已预留（collectors 接口），采集方案待定
3. **风格学习** — 暂未实现，后续可通过对比 AI 原稿和用户修改来学习
4. **Newsletter 发送** — 目前只有导出，未接发送服务
5. **Obsidian 联动** — 可通过 Studio 的导出功能间接实现
6. **知识图谱可视化** — 目前是卡片网格，后续可升级为 d3-force 力导图

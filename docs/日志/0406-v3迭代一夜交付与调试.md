# 0406 v3 迭代一夜交付与上午调试

## 迭代矩阵决策

- 从闭环六环节（采集/读/想/写/发/反馈）+ 贯穿能力 + 技术债，梳理出 30+ 个候选方向
- 洋哥的取向：个人生产力深化，分析深度"现有+合理扩展"
- 选中 8 个方向 → 拆为 3 份 Spec
- 矩阵文件：`docs/superpowers/specs/2026-04-06-iteration-matrix.md`

## 一夜三 Spec 自动交付（11 commit）

### Spec 1: 读-洞察升级（4 commit）
- B1 TL;DR：Pipeline Stage 4 末尾 AI 合成 3 件事 + 首页卡片 + 手动重跑 API
- B2 稍后读：`reading_queue` 表 + 7 天过期 lazy 清理 + 收藏页 Tabs（收藏 | 稍后读）
- C2 实体订阅：`entity_subscriptions` 表 + Pipeline 末尾检测 + Telegram 24h 去重推送
- C3 周洞察：`weekly_insights` 表 + 3 件事 + 观察 + 关键实体（手动触发，cron 未做）

### Spec 2: Studio 写作补齐（4 commit）
- D2 版本历史：`draft_versions` 表 + AI 生成前后自动 snapshot + 手动存 + 回滚（回滚前先存当前）+ 20 版本保留
- D4 预览升级：`react-markdown` + `remark-gfm` + `rehype-highlight` 替换正则 `simpleMarkdownRender`
- D5 配图生成：`ai_settings` 加 4 字段 + Gemini/OpenAI image REST API + 前端 Dialog + Settings 入口

### Spec 3: Twitter 采集（3 commit）
- Collector 多态派发架构 `pickCollector(sourceType)`
- Twitter 公开推文：包装 RSS，走 RSSHub `/twitter/user/:username`
- Twitter 私域时间线：agent-browser CLI 调用（代码就绪，待洋哥登录验证）
- 快捷配置 API + Settings 页 UI 卡片
- 单元测试 6 个

## 上午调试（8 commit）

### agent-browser CLI 校准
- 问题：昨晚假设的 `eval --url --script --json` 参数不存在
- 解决：洋哥跑 `--help` 后确认真实接口是分步命令（`open` → `wait` → `scroll` → `eval`）
- 关键：`--auto-connect` 连真实 Chrome，`--json` 放 `eval` 前面

### 小红书私域采集
- 从零实现 `xiaohongshu-private` collector
- 端到端验证通过：列表页 18 条笔记成功采集
- 链接坑：DOM 里有两种 `<a>`，hidden 的不带 xsec_token → 404。修正为只选 `a.cover`
- 正文采集：列表页只有标题 → 洋哥要求"肯定要拉正文"→ 探索了 API 拦截/HAR 等方案无果 → 最终用"逐条导航详情页从 `__INITIAL_STATE__` 取 desc"（~3s/条）

### Pipeline 修复
- RSS 每源截断 30 条（OpenAI Blog 903 条事件）
- maxItems 默认值 5→20
- 耗时显示 bug（页面恢复 pipeline 时 startTimeRef 为 0）
- 左侧源名称 custom-xxx → 动态加载源名称
- 摘要 prompt 强制中文

### 采集阶段源选择
- 洋哥要求：能选择本次跑哪些源
- 实现：DigestTrigger 新增 `selecting` 中间态，展开源复选框面板
- Pipeline 接受 `sourceIds` 参数，未选中的显示"跳过"

## 最终状态

- Build ✅ / Test 18/18 ✅ / 工作树 clean
- 19 个新 commit 未 push
- 交接文档：`docs/handoff-0406-afternoon.md`
